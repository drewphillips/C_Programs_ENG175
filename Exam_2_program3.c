//COPY and PASTED CODE FROM D2L for grading on 10/28/23

#include <stdio.h>
#include <math.h>
#include <string.h>
#include <ctype.h>

int main()
{
   int i;
   int array[i];
   
   int smallest;

while (array[i] > 0) {

   for (i=0; i < strlen(array[i]); i++);
       scanf("%d", array[i]);

   for (i =0; i < strlen(array[i]); i++) {
       if smallest > array[i];
       smallest = array[i];
   }

   printf("the smallest value was %d", smallest);

}
   printf("Program #3\n\n");



 //Write a C program that takes any number of non-negative integers as input, and outputs the smallest integer.  Loop until the user wants to quit.

//A negative integer ends the input and is not considered for being the smallest.

//If the input is 99 3 5 -1, the output is 3, the smallest integer of 99 3 and 5.
//If the input is 4 2 8 6 9 5 -7, the output is 2, the smallest integer of 4 2 8 6 9 5.

   return 0;
}


/*
//COPY and PASTED CODE FROM D2L for grading on 10/28/23

//code does not compile and run

(5 pts) takes any number of non-negative integers as input
(5 pts) outputs the smallest integer
loops until the user wants to quit.
*/